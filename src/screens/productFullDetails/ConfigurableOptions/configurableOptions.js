import React, { memo } from 'react';

const ConfigurableOptions = (props) => {
  const { configurable_options: configurableOptions } = props;
  return <></>;
};

export default memo(ConfigurableOptions);
